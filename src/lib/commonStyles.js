import { css } from '@emotion/core';

export const containerPadding = css`
  padding: 20px 50px;
`;

export const container = css`
  ${containerPadding}
  flex-grow: 1;
`;

export const colors = {
  primary: '#000',
  navActive: '#333',
  white: '#fff',
}; 